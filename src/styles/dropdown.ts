import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
  block: {
    marginVertical: 15,
  },
  containerStyle: {
    marginTop: 30,
    width: 200,
    height: 65,
  },
  dropDownStyle: {
    marginTop: 15,
  },
});
